import { urls } from './urls'
import { rpcErrorCodes } from './rpc-error-codes'
import { rpcDefaults } from './rpc-defaults'
import { attributeTypes } from './attribute-types'
import { socialHandlers } from './social-handles'

const constants = {
  urls,
  rpcErrorCodes,
  rpcDefaults,
  attributeTypes,
  socialHandlers,
}

export { constants }
